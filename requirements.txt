# แพ็กเกจสำหรับตัวเว็บอย่างเดียว — ไม่รวมส่วน RAG
#
# แยกไว้เพราะ Vercel จำกัดขนาด serverless function ไว้ที่ 500 MB ส่วน torch กับ
# sentence-transformers รวมกันเกิน 5 GB ถ้าใส่รวมไว้ในไฟล์นี้ deploy จะพังทันที
#
# ติดตั้งครบทุกอย่างสำหรับรันในเครื่อง (รวม RAG) ให้ใช้:
#     pip install -r requirements-rag.txt
fastapi>=0.141.1
httpx>=0.28.1
jinja2>=3.1.6
pydantic-settings>=2.15.0
python-dotenv>=1.2.2
uvicorn[standard]>=0.52.1
